import { StyleSheet } from "react-native";

const sx = StyleSheet.create({
  snackbarContainer: {
    backgroundColor: 'black',
    position: 'absolute',
    bottom: 0,
  }
});

export { sx }
