import { StyleSheet } from "react-native";

const sx = StyleSheet.create({
	root: {
		flex: 1,
		position: 'relative'
	},
  snackbarContainer: {
    backgroundColor: 'black',
    position: 'absolute',
    bottom: 0,
  }
});

export { sx }
